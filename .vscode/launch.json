{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug in Chrome (run `npm run dev` first)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
